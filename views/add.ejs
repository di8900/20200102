<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=<device-width>, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>add page</title>
</head>
<body>
    <h1><a href="/topic">네이버 실시간 순위</a></h1>
    <ol>
        <% for(var i=0; i<topics.length; i++) { %>
            <li>
                <a href="/topic/<%=topics.id %>">
                <%= topics[i].title %></a> : <%= topics[i]. description %> 
            </li>
            <% } %>
    </ol>
    <article>
        <form method="POST" action="/topic/add">
            <p><input type="text" name="title" value="<%= topics.title %>" placeholder="title"/></p>
            <p><textarea name='description' rows"6" cols="50" placeholder="description" value="<%= topics.title %>"></textarea>
            <p><input type="text" name="author" value="<%= topics.author %>" placeholder="author"/></p>
            <p><input type="submit"/></p>
        </form>
        <ul>
            <li><a href="/topic/add">add</a></li>
        </ul>
    </article>
</body>
</html>